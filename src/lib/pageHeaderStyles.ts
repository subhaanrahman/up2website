/**
 * Mobile headers for primary tab surfaces (Tickets, Search, Dashboard messages/chats).
 * Single source of truth — keep Tickets / Events / AppHeader tabMain in sync.
 */
export const MOBILE_TAB_HEADER_CLASS = "bg-background px-4 pt-6 pb-2 z-40";

export const MOBILE_TAB_HEADER_TITLE_CLASS =
  "text-2xl font-bold text-foreground mb-4 text-center uppercase";
